import * as Icons from "lucide-react";
const Save = (Icons as any).Save || (() => null);
const Eye = (Icons as any).Eye || (() => null);
const Upload = (Icons as any).Upload || (() => null);
const X = (Icons as any).X || (() => null);
const Plus = (Icons as any).Plus || (() => null);
const Trash2 = (Icons as any).Trash2 || (() => null);
const ImageIcon = (Icons as any).Image || (() => null);
const Heading = (Icons as any).Heading || (() => null);
const Type = (Icons as any).Type || (() => null);

export default function NewBlogPage() {
  return (
    <div>
      <h1>Create New Blog</h1>
    </div>
  );
}